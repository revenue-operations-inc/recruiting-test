{
  "name": "mock-sdr-api",
  "version": "0.1.0",
  "private": true,
  "description": "Mock data layer for an AI SDR system targeting mortgage lenders.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "walkthrough": "ts-node examples/walkthrough.ts",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
